# Claude Resume Analyzer

This Streamlit app compares a resume and job description using Claude AI. Toggle `USE_CLAUDE` to True to enable real API access.

To run locally:
```bash
streamlit run app.py
```
